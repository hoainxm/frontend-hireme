/** @format */

import React, { FC } from "react";

interface Props {
  id: string;
}

const APIKeyInfo: FC<Props> = (props: Props) => {
  return <div>This feature is on developing</div>;
};

export default APIKeyInfo;
